name: Deploy Web App to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'Data/Processed/**'
      - 'frontend/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: Generate pizza stats CSV
        run: |
          python3 -m pip install -q pandas
          python3 streamlit/generate_pizza_csv.py

      - name: Copy data files to public/data/
        run: |
          mkdir -p frontend/public/data
          cp Data/Processed/player_scores_all_seasons_long.csv frontend/public/data/
          cp Data/Processed/player_scores_agg_by_player.csv   frontend/public/data/
          cp Data/Processed/squad_scores_all_seasons.csv       frontend/public/data/
          cp Data/Processed/player_market_values.csv           frontend/public/data/
          cp Data/Processed/player_pizza_all_seasons.csv       frontend/public/data/
          cp Data/Processed/player_sofa_metrics.csv            frontend/public/data/
          cp Data/Processed/player_shots-*.csv                 frontend/public/data/

      - name: Copy club crests
        run: |
          mkdir -p frontend/public/crests
          cp Data/Assets/club_crests/*.png frontend/public/crests/

      - name: Build
        working-directory: frontend
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: frontend/dist
          publish_branch: gh-pages
